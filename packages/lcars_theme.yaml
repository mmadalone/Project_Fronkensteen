# =============================================================================
# LCARS Theme Helpers
# =============================================================================
# Non-AI helpers for the LCARS dashboard theme.
# Provides: 2 input_boolean entities (lcars_sound, lcars_texture)
# =============================================================================

input_boolean:
  lcars_sound:
    name: LCARS Sound
    icon: mdi:volume-high
    initial: false

  lcars_texture:
    name: LCARS Texture
    icon: mdi:texture
    initial: false
